name: at_locals_app
description: "At Locals — Somalia Hotels & Apartments"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  shared_preferences: ^2.3.2
  cached_network_image: ^3.4.1
  go_router: ^14.6.2
  intl: ^0.19.0
  provider: ^6.1.2
  flutter_svg: ^2.0.10
  geolocator: ^13.0.4
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.svg
